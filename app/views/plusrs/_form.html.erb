<%= form_for(@plusr) do |f| %>
  <% if @plusr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@plusr.errors.count, "error") %> prohibited this plusr from being saved:</h2>

      <ul>
      <% @plusr.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :plan_id %><br>
    <%= f.text_field :plan_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
