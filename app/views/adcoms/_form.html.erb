<%= form_for(@adcom) do |f| %>
  <% if @adcom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adcom.errors.count, "error") %> prohibited this adcom from being saved:</h2>

      <ul>
      <% @adcom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :admin_id %><br>
    <%= f.text_field :admin_id %>
  </div>
  <div class="field">
    <%= f.label :company_id %><br>
    <%= f.text_field :company_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
